import css from './interfaceFeedback.module.css';
import PropTypes from 'prop-types';

export default function InterfaceFeedback () {
    return (
      <section className={css.statistics}>
        
      </section>
    );
  }